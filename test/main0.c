#include <stdio.h>
#include <block.h>
#include <vstr.h>
#include <vstring.h>
#include <vfs.h>
#include <vstack.h>
#include <vdef.h>

int main() 
{   
    // vstack * s = vstack_create(int, 2, .75, NULL);
    
    // int d = 2;
    
    // vstack_push(s, &d);
    // vstack_push(s, &d);
    // vstack_push(s, &d);
    // vstack_push(s, &d);
    // vstack_pop(s, 0);
 
    // int * e = (int*)vstack_peek(s, 0);
    // printf("%d",  *e);
    // e = (int*)vstack_peek(s, 1);

    // #define vMAX(a, b) ({ \
    //     __typeof__(a) _a = (a); \
    //     __typeof__(b) _b = (b); \
    //     _a > _b ? _a : _b; \
    // })


    
    return 0;
}